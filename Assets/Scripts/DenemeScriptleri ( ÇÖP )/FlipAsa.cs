using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlipAsa : MonoBehaviour
{
    public Transform asaPoint;
    //private bool sayac;

    /*private void Update()
    {
        controller _controller = GetComponent<controller>();
        Debug.Log(_controller.yön);
        Vector2 asaPosition = new Vector2(asaPoint.position.x, asaPoint.position.y);
        if (_controller.yön == -1.0f)
        {
            asaPosition.x = -1.06f;
        }

        if (_controller.yön == 1.0f)
        {
            asaPosition.x = 1.06f;
        }

        asaPoint.position = asaPosition;
    }*/
}
